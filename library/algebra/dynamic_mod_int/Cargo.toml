[package]
name = "dynamic_mod_int"
version = "0.1.0"
edition = "2018"

[dependencies]
prelude = { path = "../../prelude" }
algebra = { path = "../../algebra" }

[dev-dependencies]
rand = "0.6"

[lib]
path = "lib.rs"
